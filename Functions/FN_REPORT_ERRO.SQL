CREATE OR REPLACE FUNCTION FN_REPORT_ERRO(DESCRICAO TEXT, STATE TEXT, ERRO TEXT)
RETURNS VOID AS $$
BEGIN						  
	RAISE INFO '%', DESCRICAO;
	RAISE NOTICE '% %', STATE, ERRO;
	
	EXCEPTION WHEN OTHERS THEN
		RAISE INFO 'ERRO NO ERRO % %', sqlstate, sqlerrm;
END;
$$  LANGUAGE PLPGSQL